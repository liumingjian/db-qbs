# M3 渲染面人工走查清单

**规格来源**：`ADR-0032`
（决策票 [#103](https://github.com/liumingjian/db-qbs/issues/103)）。
界面增量的定形见 [#102](https://github.com/liumingjian/db-qbs/issues/102) 与
[`docs/prototypes/0102-m3-ui-increments.html`](../../../prototypes/0102-m3-ui-increments.html)，
裁定落 `ADR-0025`、
`ADR-0010`、
`ADR-0027`。
视觉规则的唯一来源仍是 [`docs/design-system/`](../../../design-system/)。

## 这份清单为什么存在，以及为什么**不是**重跑 V1–V25

#102 已判 **M3 零设计系统改动**——`docs/design-system/README.md` 与 `tokens.css`
一个字没改，五处新信息位全部由既有元素承载。因此
`ADR-0028` 的
**第 2 条触发条件不成立，整份 `m2-visual-walkthrough.md` 不重跑**。

要看的只有两类东西：**五处新信息位真的渲染出来了**，以及**一处 `web/src/app.css`
的布局裁定真的生效了**。六条，就是本清单。

**不并进 M2 那份**：并进去之后「跑过 M2 走查」这句话的含义会随 M3 漂移，
理由与 ADR-0032 §2 的三入口分立同源。

**不给退出码。** 自动验收（`run-m3-acceptance.sh` 的 B 系列）与本清单互不替代：
前者判「值搬对没有、报告说对没有」，后者判「渲染出来没有」。

## 触发条件（两条，固定）

1. **M3 验收时必跑一次。**
2. **任何改动 `web/src/app.css` 中 `.precheck-reports` 布局、或 `DiagnosticTable`
   列结构的变更，合并前必跑一次。**

跑完**贴实际观察，不是贴「通过」**——照 ADR-0014 §8 第 3 条的先例。

## 2026-08-21 增补（[ADR-0043](../../../adr/0043-p2-job-center.md)）

P2 把 `web/src/app.css` **整体重写**（不是增补），触发条件第 2 条因此命中，W1–W6 必跑。
真跑的结果落在 `m3-visual-walkthrough-20260821T051427Z.md`。

跑出来一件事：**W3 / W4 / W5 的对象已经不存在**——`47a2fed`
（*Prepare x2doris P1 frontend handoff*）把构建器里整段「目标表建表 SQL / 拿建表 SQL /
`.fetch-ready`」摘掉了，而那一票没跑本清单（`CLAUDE.md` 规则 1 挡的正是这个）。
所有者 2026-08-21 裁定**按有意的收窄对待，三条判据就此判废**，理由与时效落在 ADR-0043
「所有者 2026-08-21 的两条收尾裁定」。**编号一个不重编、一行不删**，处置写在下表各自那一行里。

W1 / W2 / W6 判据一字未动。

## 走查项

制造情形的手段与自动验收同源（ADR-0032 §4 的 B1–B6），走查时可直接复用那套编排造出各态；
`M3_KEEP_RIG=1` 的用法照 M2 那份。

| # | 打开哪一屏 · 制造什么情形 | 该看见什么 |
|---|---|---|
| **W1** | 运行详情 · 映射预检失败（复用 **B2** 的全违规表） | 预检表是**五列**，第五列「建议」**一律填动作、没有空格子**（web 不重算，值由 sink 侧填）。逐列一条 + 总计，**一次报全**，不分组、不折叠、不截断 |
| **W2** | 同上，视口 **1024** 与 **1440** 各看一次 | 映射预检失败态下两栏**整宽堆叠**，第五列**全在框内、不需要横滚**（ADR-0025 增补 §3）。对照：`shape-failed` 态**仍是两栏并置**，不得一起改掉 |

> **2026-08-19 订正（[#121](https://github.com/liumingjian/db-qbs/issues/121)）**：W2 那句对照
> **失去对象**——SQL 形状预检整段随 `ADR-0036` §5 取消，
> `shape-failed` 这个态在 v1 不存在了，`.precheck-reports` 只剩映射预检一段（整宽）。
> W2 现在只看「整宽 + 第五列不横滚」这半句。
> 另：**造态手段也要重挑**——B1–B6 那套编排仍是退役形态，改造归
> [#122](https://github.com/liumingjian/db-qbs/issues/122)。在它就绪前，走查用桩后端喂
> `web/dist` 造态，并在记录里明写这处偏差（见 `m3-visual-walkthrough-20260819T025634Z.md`）。
| **W3** | ~~构建器 · 取列~~ | **N/A（判据已随 [ADR-0043](../../../adr/0043-p2-job-center.md) 退役）**。原判「取列卡三档标记 `[待配精度]` / `[不支持]` 是纯文字、不着色」——**取列卡整段已不存在**（`47a2fed` 摘掉，所有者 2026-08-21 裁定按有意的收窄对待）。「标记是纯文字不是标签」这条**态度没退役**，同构的判据在 W3 之外还有 X7（长度单位那句静态说明）守着。编号保留 |
| **W4** | ~~同上，看建表 SQL 区块~~ | **N/A（判据已随 [ADR-0043](../../../adr/0043-p2-job-center.md) 退役）**。第一版**不提供建表 SQL**，目标表仍由用户自己在目标库建好；`DECIMAL(<p>,<s>)` 占位符与「整份 DDL 照给」都没有对象。编号保留 |
| **W5** | ~~构建器 · 取列（白名单外的列）~~ | **N/A（判据已随 [ADR-0043](../../../adr/0043-p2-job-center.md) 退役）**。第四态的切换入口本身长在取列卡上，卡没了这一态无从制造。**「判定归 sink 侧硬拒」没有变**（ADR-0009），映射预检仍会拦住这类列，由 W1 / W6 守。编号保留 |
| **W6** | 运行详情 · 映射预检失败（复用 **B4** 的超域裸 `NUMBER` 表） | 值域校核的不合规记录**混在同一张五列表里**，与其余逐列规则**形态一致**（ADR-0030 §4.3），不另起区块、不另起标题 |

## 记录格式

每次走查在本目录开一份 `m3-visual-walkthrough-<UTC>.md`（与 `m3-acceptance-<UTC>.md` 并列），
逐条写实际观察。
**「W2 通过」不算记录，「W2：1024 下两栏已堆叠，第五列『建议』末列右边距 16px 在框内；
1440 下同样堆叠，未回退两栏」才算。**
