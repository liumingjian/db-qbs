# 第一版 rig 验收报告（C1-C6）

- Generated (UTC): 2026-08-19T14:13:34Z
- Git commit: eab73c5cc24be34ac2089906391b66ef826d87cd
- 判据依据：ADR-0040 §4（C 系列）与 §3（内存形状）。判据不在本报告里重新推导。
- 第一版验收是**四份**台架（ADR-0040 §5.4）：M1 / M2 / M3 / 本入口。本报告只主张 C1-C6。

## 客户五条需求的兑现对照（ADR-0034 / STRATEGY-V1）

| # | 客户需求 | 兑现在哪 | 本次结果 |
|---|---|---|---|
| 1 | 界面上管数据源（Oracle 源 + MySQL 目标，含测试连接） | C1 | PASS |
| 2 | 字段映射与目标字段/主键选择 | C2（映射跑通）、C5（配错拦住）、C4⑤（无唯一约束拒跑） | C2=PASS / C5=PASS / C4=PASS |
| 3 | 用户可填筛选条件 | C3 | PASS |
| 4 | 主键 upsert，不再整段删重刷 | C4 | PASS |
| 5 | 单次 10 万行 / 约 100MB，行数对得上，内存不随数据量线性增长 | **分两处**：行数与行宽在 M1 的 `wide-100k`（本入口不跑，见同批次 m1-acceptance 报告的载荷记账行）；内存形状在 C6 | C6=PASS；M1 那一半本入口未跑 |

> 另有一条「不做自动建表」（目标表由人预建）是已生效的禁令（ADR-0033 §4），不设检查项。

## Scenarios

| Scenario | Result |
|---|---|
| C1-datasource-crud | PASS |
| C2-column-mapping | PASS |
| C3-user-conditions | PASS |
| C4-upsert-idempotence | PASS |
| C5-precheck-branches | PASS |
| C6-memory-shape | PASS |

## C6 内存高水位（四个绝对数 + 两组基线）

| 进程 | 档 | 基线 (B) | 峰值 (B) | 增量 (B) | 量法 |
|---|---|---:|---:|---:|---|
| source | 10k | 24428544 | 124694528 | 100265984 | wait4 ru_maxrss |
| source | 100k | 24309760 | 24309760 | 0 | wait4 ru_maxrss |
| sink | 10k | 5062656 | 74543104 | 69480448 | /proc/<pid>/status VmHWM |
| sink | 100k | 6963200 | 91119616 | 84156416 | /proc/<pid>/status VmHWM |

- **source 斜率**：0 / 100265984 = **0.00**（判据 ≤ 2.00）
- **sink 斜率**：84156416 / 69480448 = **1.21**（判据 ≤ 2.00）
- **sink 重启**：10k 档 pid=13556，100k 档 pid=13611，两档之间确实重启过
- **耗时（只记不判，ADR-0040 §1/§3）**：10k 档 7s，100k 档 49s

- 量的是内核维护的单调高水位（source: `wait4` 的 `ru_maxrss`；sink: `/proc/<pid>/status` 的 `VmHWM`），**不是轮询采样**——采样会漏掉峰值，漏掉之后判据假绿（ADR-0040 §3.1）。
- 单位：source 侧 `ru_maxrss` 在 macOS 是字节、在 Linux 是 kB，wrapper 已归一到字节；sink 侧 `VmHWM` 是 kB，报告里已乘 1024。
- **不设绝对上限**（ADR-0040 §3.4，现场独立服务器内存充裕）；**耗时只记不判**（夜间批量）。

## Assertion evidence

### C1-datasource-crud

```text
==> stopping stale processes on 18088: 39894 
GET /api/tasks: 200
C1① oracle datasource created: true
C1① target datasource created: true
C1① both kinds present: true
C1② bad-credential test-connection rejected: false
C1② rejected by the credential, not by the request shape: false
C1② failure carries a message: true
C1② datasource count unchanged: 2
C1② test-connection status=502 body={"kind":"sink","message":"连接目标端失败：连接 MySQL 失败：MySqlError { ERROR 1045 (28000): Access denied for user 'spike'@'172.27.0.4' (using password: YES) }"}
C1③ no password-ish key in view: 0
C1③ no plaintext credential in view: false
C1③ has_password reported instead: true
C1④ delete referenced datasource: 409
C1④ report names the task: true
C1④ message repeats the task name: true
C1④ 409 body: {"error":{"message":"数据源仍被 1 个任务引用：C1 引用检查（删除会被 409 拒）；请先改这些任务的数据源","tasks":["C1 引用检查（删除会被 409 拒）"]}}
C1⑤ rename without test-connection: 200
C1⑤ new name stored: V1 改名用（已改名）
C1⑤ password kept: true
C1⑤ unreferenced datasource deletes cleanly: 200
```

### C2-column-mapping

```text
GET /api/tasks: 200
C2① outcome: SUCCEEDED
C2① target effect: SWAPPED
C2① target rows: 3
C2① DEST_NAME by target name: alpha
bravo
delta
C2① DEST_AMOUNT by target name: 10.25
20.50
30.75
C2② builder columns status: 200
C2② source column names available for prefill: true
C2② 预填这一步在构建器里（ADR-0039 §5），台架只证输入齐备；渲染面归 X 走查
C2③ /target/tables status: 200
C2③ tables list contains V1_C2: true
C2③ /target/columns status: 200
C2③ column count: 4
C2③ keys report the primary key: true
C2③ missing table is not an error: 200
C2③ missing table returns an empty list: 0
C2③ target columns: [{"character_set":null,"column_type":"decimal(8,0)","data_type":"decimal","datetime_precision":null,"default_value":null,"extra":"","length":null,"name":"ROW_ID","nullable":false,"ordinal":1,"precision":8,"scale":0},{"character_set":"utf8mb4","column_type":"varchar(80)","data_type":"varchar","datetime_precision":null,"default_value":null,"extra":"","length":80,"name":"DEST_NAME","nullable":true,"ordinal":2,"precision":null,"scale":null},{"character_set":null,"column_type":"decimal(10,2)","data_type":"decimal","datetime_precision":null,"default_value":null,"extra":"","length":null,"name":"DEST_AMOUNT","nullable":true,"ordinal":3,"precision":10,"scale":2},{"character_set":null,"column_type":"datetime","data_type":"datetime","datetime_precision":0,"default_value":null,"extra":"","length":null,"name":"LOAD_DATE","nullable":true,"ordinal":4,"precision":null,"scale":null}]
C2③ target keys: [{"columns":["ROW_ID"],"name":"PRIMARY"}]
```

### C3-user-conditions

```text
GET /api/tasks: 200
C3① constant condition outcome: SUCCEEDED
C3① rows after constant run: 3
C3① all rows are group A: 3
C3① runtime condition outcome: SUCCEEDED
C3① group B rows after runtime run: 2
C3① total rows after both runs: 5
C3② builder sql status: 200
C3② constant is bound, not inlined: true
C3② generated SQL (constant condition):
SELECT a.ROW_ID AS ROW_ID,
       a.GRP AS GRP,
       a.LOAD_DATE AS LOAD_DATE
  FROM SPIKE.T_V1_C3 a
 WHERE a.LOAD_DATE = TO_DATE(:load_date,'YYYY-MM-DD')
   AND a.GRP = :grp
C3② runtime value is bound too: true
C3② generated SQL (runtime condition):
SELECT a.ROW_ID AS ROW_ID,
       a.GRP AS GRP,
       a.LOAD_DATE AS LOAD_DATE
  FROM SPIKE.T_V1_C3 a
 WHERE a.LOAD_DATE = TO_DATE(:load_date,'YYYY-MM-DD')
   AND a.GRP = :grp
C3③ hand-written SQL is refused by the protocol: 400
C3③ refusal body: {"error":{"message":"JSON 请求体无效：unknown field `source_sql`, expected one of `dblink`, `owner`, `table`, `target_table`, `primary_key`, `columns`, `conditions`, `order_by` at line 1 column 470"}}
C3③ 界面上没有手改 SQL 的控件这一半归 X 走查，本报告不主张已观察
```

### C4-upsert-idempotence

```text
GET /api/tasks: 200
C4① first run outcome: SUCCEEDED
C4① rows after first run: 5
C4① second run outcome: SUCCEEDED
C4① rows after second run: 5
C4② staged rows: 5
C4② staged <= affected: 5 <= 5
C4② affected <= 2x staged: 5 <= 10
C4③ purged rows: 0
C4④ third run outcome: SUCCEEDED
C4④ changed source value landed: first-v2
C4④ row count still unchanged: 5
C4⑤ sink code: PRECHECK_FAILED
C4⑤ failure kind: MAPPING_PRECHECK
C4⑤ rule names the missing unique constraint: true
C4⑤ target untouched: DISCARDED
C4⑤ no staging table left: 0
C4⑤ mapping issues: [{"column":"<primary_key>","message":"目标端：目标表上必须有一条 PRIMARY KEY 或 UNIQUE 约束，其列集合与勾选的主键完全一致；否则 ON DUPLICATE KEY UPDATE 会静默退化成纯 INSERT，重跑就出重复行","rule":"目标表上必须有一条 PRIMARY KEY 或 UNIQUE 约束，其列集合与勾选的主键完全一致；否则 ON DUPLICATE KEY UPDATE 会静默退化成纯 INSERT，重跑就出重复行","source":"ROW_ID","suggestion":"在目标表上建对应的主键或唯一索引，或改勾主键列","target":"<无唯一约束>"}]
```

### C5-precheck-branches

```text
GET /api/tasks: 200
C5① sink code: PRECHECK_FAILED
C5① rejected on the primary key column: true
C5① target untouched: DISCARDED
C5① mapping issues: [{"column":"ROW_ID","message":"目标端：主键列必须 NOT NULL：MySQL 的 UNIQUE 允许多个 NULL，可空会让 upsert 静默退化成纯 INSERT","rule":"主键列必须 NOT NULL：MySQL 的 UNIQUE 允许多个 NULL，可空会让 upsert 静默退化成纯 INSERT","source":"-","suggestion":"把目标列改成 NOT NULL","target":"DECIMAL(8,0)"}]
C5② outcome: SUCCEEDED
C5② mapping issues: 0
C5② target rows: 2
C5② unmapped column with a default was filled by MySQL: 0
C5③ sink code: PRECHECK_FAILED
C5③ rejected on the unmapped required column: true
C5③ source column reads 未映射: （未映射）
C5③ no staging table left: 0
C5③ mapping issues: [{"column":"MUST_FILL","message":"目标端：目标表的 MUST_FILL 列未被映射且不允许留空，请映射它或给它默认值","rule":"目标表的 MUST_FILL 列未被映射且不允许留空，请映射它或给它默认值","source":"（未映射）","suggestion":"映射这一列，或在目标表上给它一个默认值","target":"VARCHAR(20)"}]
```

### C6-memory-shape

```text
GET /api/tasks: 200
C6 10k: sink 已重启，pid=13556（VmHWM 跨 run 只增不减，不重启比值恒为 1）
C6 10k baseline: source ru_maxrss=24428544 B, sink VmHWM=5062656 B
C6 10k peak: source ru_maxrss=124694528 B, sink VmHWM=74543104 B, rows=10000, 7s
C6 100k: sink 已重启，pid=13611（VmHWM 跨 run 只增不减，不重启比值恒为 1）
C6 100k baseline: source ru_maxrss=24309760 B, sink VmHWM=6963200 B
C6 100k peak: source ru_maxrss=24309760 B, sink VmHWM=91119616 B, rows=100000, 49s
C6 tier rows (10k): 10000
C6 tier rows (100k): 100000
C6 target rows after both tiers: 100000
C6 sink was restarted between tiers: true
C6 source 10k delta is measurable: 100265984 > 0
C6 sink 10k delta is measurable: 69480448 > 0
C6 source slope: 0 <= 200531968
C6 sink slope: 84156416 <= 138960896
| 进程 | 档 | 基线 (B) | 峰值 (B) | 增量 (B) | 量法 |
|---|---|---:|---:|---:|---|
| source | 10k | 24428544 | 124694528 | 100265984 | wait4 ru_maxrss |
| source | 100k | 24309760 | 24309760 | 0 | wait4 ru_maxrss |
| sink | 10k | 5062656 | 74543104 | 69480448 | /proc/<pid>/status VmHWM |
| sink | 100k | 6963200 | 91119616 | 84156416 | /proc/<pid>/status VmHWM |

- **source 斜率**：0 / 100265984 = **0.00**（判据 ≤ 2.00）
- **sink 斜率**：84156416 / 69480448 = **1.21**（判据 ≤ 2.00）
- **sink 重启**：10k 档 pid=13556，100k 档 pid=13611，两档之间确实重启过
- **耗时（只记不判，ADR-0040 §1/§3）**：10k 档 7s，100k 档 49s
```

## 台架能证到哪儿（本报告不冒充证过的两处）

- **C1②「测通才让存」**：服务端的 `POST /api/datasources` 本身不测连，这道门槛在数据源对话框上（ADR-0039 §3）。本入口证的是它依赖的事实——错凭据的测连确实失败、失败之后库里没多出一条；**对话框那一半归 X 走查**。
- **C2②「默认预填同名」与 C3③「界面无手改 SQL 入口」**：预填与控件都在前端（ADR-0039 §5）。本入口证的是协议面——取列面回的列名齐备、任务定义收不下裸 SQL（`deny_unknown_fields` 400）；**渲染那一半归 X 走查**。

## 三份视觉走查在本入口的触发情况

- **V1-V25 / W1-W6 / X1-X8：本入口未跑。** 本票（#135）只动 `docs/spikes/fixtures/local-rig/` 下的脚本与 fixture，不碰设计系统、`.precheck-reports` 布局、`DiagnosticTable` 列结构，也不碰数据源屏与构建器——`CLAUDE.md` 那张表的三条触发条件一条都没命中。X1-X8 的复跑挂在第一版整体验收票（#136）上。
