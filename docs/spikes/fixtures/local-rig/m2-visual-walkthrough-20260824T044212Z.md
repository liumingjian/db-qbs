# 设计系统走查实录 · V1–V25（2026-08-24，目标端 Agent 注册表）

- **触发**：`CLAUDE.md` 视觉门禁表第 1 行——`docs/design-system/README.md` §7 的屏幕清单
  加了「目标端 Agent 屏」那一条（[ADR-0044](../../../adr/0044-target-agent-registry.md) §走查触发）。
  **按文件命中，不按改动大小命中**：改了它就跑，「这只是一段文字」正是规则 1 不许说的话。
- **这一趟跑的是回归**：`tokens.css` 与 `web/src/app.css` **一个字未改**，
  新屏只用既有的 `.card` / `.data-grid` / `.state` / `.row-actions`，组件清单不增减。
  预期是「上一轮什么形状，这一轮还是什么形状」，实测确实如此。
- **怎么跑的**：`walkthrough/run-v-walkthrough.sh`（`v-mock.py` + `v-probe.py`），
  真跑在用户 mac 上（`rexec`，`lmj-mac-mini-269d`），喂真实的 `web/dist`。
  这台 mac 是第一次跑走查，`~/pwvenv` 与 `pillow`（V5 的灰度取样要它）是这次现装的。
- **判据版本**：`m2-visual-walkthrough.md`，含 2026-08-19 的 N/A 处置与 2026-08-21 的 P2 改判。

原始输出：mac 上的 `/tmp/v-report.json`（27 组观察）。下面是实际观察，不是「通过」。

---

## 与本票直接相关的那一条：V24（外壳）

```
sidebar_bg: rgb(0, 21, 41)
sidebar_items: 目标端 Agent · 作业中心(is-active) · 数据源 · 系统设置   ← 四项
collapsed_icon_centering: 展开块宽 236 / 折叠块宽 40 / 侧栏 48 / icon_center_offset 0
builder_hits_in_nav: 0
```

**V24 不改判**：它判的是外壳**形态**——深色侧栏、折叠时图标居中、没有灰标占位项——
**导航有几项不在它的判据里**。四项与三项在这一条上是同一个形状：同一套 `A.nav-item`、
同一个选中块、`icon_center_offset` 仍是 0（图标居中，不是被切掉大半）。
**导航项集合归 X1 守**，这一轮 X1 已按四项改判并实测。

## 其余各条（逐条实测，形状与上一轮一致）

- **V1**：阶段串四项，`PREPARING 准备中` 已完成绿点 `rgb(82,196,26)`、
  `STREAMING 传输中` 当前蓝点 `rgb(60,126,255)`，圆点 8×8、`radius 50%`。
- **V2**：`SWAPPED | 目标表已切换` 一块，`terminal-block is-swapped`，
  **实心**绿底 `rgb(205,235,217)` / 字 `rgb(11,102,55)`；同屏错误码标签 0、阶段项 0。
- **V3**：`DISCARDED | 目标表未被触碰`，`is-discarded`，**背景透明**（描边态）、
  字 `rgba(0,0,0,.65)`；同屏 1 个错误码标签。**轴二两态一实一描，没有混**。
- **V5（可量的那条）**：整页 `grayscale(1)` 后，`SWAPPED` 块中位亮度 **227**、
  `DISCARDED` **255**，差 **28（11.0%）**，仍在 ≥25/255 的下界之上。
- **V4**：`PRECHECK_FAILED HTTP 422` 是 `error-code is-rejected`（红字虚线框），
  右边跟人话 `[类型映射] 目标端：映射预检未通过：一次发现 3 项问题，未创建暂存表`；
  抽屉里那条是 `VERIFY_FAILED | HTTP 409`，同样 `dashed` 边。
  5xx 那条是 `INTERNAL_PRECHECK_ESCAPE | HTTP 500`，`is-internal`（黄底实线），
  **与 4xx 分得开**。
- **V7**：只出错误码标签，终态块 0、阶段项 0。
- **V8（结局不明）**：`结局不明 | 进程消失，无终态日志 | 无法确认目标表是否被修改，请到目标库核对。`
  ——**只有人话结论条，终态块 0 / 错误码标签 0**。
- **V11**：预检报告**只剩一段**（`sections: 1`，`is-failed`），表头 `映射预检 | 目标端`，
  列 `列 · 源端 · 目标端 · 规则 · 建议`，三行逐字带列名与建议。
- **V13**：真实业务值默认 `filter: blur(5px)` + `user-select: none`，按钮写「显示」；
  点开后 `filter: none`。**显示即把源库真实值送进这台浏览器**那句提示在。
- **V15 / V14**：抽屉标题旁 `rec-verify`（`ui-monospace` 12px 灰），
  正文里 `目标端运行号 = 20260819121000_cccccc`；未发起那条写
  `未发起，目标端不知道这次运行`——**两个 id 谁也不替代谁**。
- **V16**：陈旧 run 提示 `stale-run-hint`，黄底 `rgb(255,251,230)`、无边框，
  文案 `该任务以同一组运行参数可能已有一次运行正在进行。| rec-live · 已跑 112 小时 40 分钟 | 状态可能有延迟，以发起结果为准。`
- **V17**：「取消运行」是 `button is-ghost`（白底、灰边），**不是主色按钮**。
- **V22**：出口文案 `目标表结构与本次取数的列对不上。请在目标库中调整目标表，或回到任务编辑修改字段映射。`
  + 一个「编辑任务」按钮；`create_table_hits: 0`（**不诱导去建表**）。
- **V23**：运行屏 `重试 0 / 重新发起 1`；任务屏两者都是 0。
- **V21**：目标表是 `<input list>` + 一个 `<datalist>`；长度栏那句静态说明在，
  `not_drawn_copy_hits: 0`。
- **V18**：`textarea` 0 个，SQL 区写 `源端 SQL | 只读预览`，
  没有「手改」徽标、没有「重走向导」入口。
- **V19 / V20**：`N/A（判据已随 ADR-0043 退役）`——构建器里没有「拿建表 SQL」按钮
  与 `.fetch-ready` 区块，整段在 `47a2fed` 被摘掉。探针原样打印了这个缺席理由。
- **V25**：`--weight-em` 实测 **500**，页面只用 `400 / 500` 两档；
  `dark_media_matches: false`、暗色条件规则 0——**第一版没有暗色主题**。
- **RETIRED 组**：V6 / V9 / V10 / V14 的对象缺席理由由探针原样打印，
  与 2026-08-19、2026-08-21 两次的处置逐字一致。

## 结论

**V1–V25 无改判、无新增、无回归**。本票对设计系统的全部影响就是 README §7 多了一行屏幕清单；
新屏的渲染面判据在 X19（`v1-visual-walkthrough-20260824T044212Z.md`），不进本清单。
