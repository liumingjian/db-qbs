# 一次性 spike 程序（#5）—— 不进主干构建，故意不放进任何 workspace。
[package]
name = "spike-bulk"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# 与 #3 同一驱动：封装 ODPI-C 走官方 OCI。
oracle = "0.6"

[[bin]]
name = "spike-bulk"
path = "src/main.rs"

[profile.release]
debug = false
