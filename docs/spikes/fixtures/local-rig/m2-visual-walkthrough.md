# M2 渲染面人工走查清单

**规格来源**：[ADR-0028 §6](../../../adr/0028-m2-acceptance-criteria-and-rig-extension.md)
（决策票 [#59](https://github.com/liumingjian/db-qbs/issues/59)）。
屏与态的定形见 [#58](https://github.com/liumingjian/db-qbs/issues/58) 与
[`docs/prototypes/0058-m2-full-flow-prototype.html`](../../../prototypes/0058-m2-full-flow-prototype.html)。
视觉规则的唯一来源是 [`docs/design-system/`](../../../design-system/)。

## 这份清单为什么存在

ADR-0028 §1 把 M2 的自动断言面画在 `source` 的 `/api/*` 上，**不上浏览器自动化**——
DOM 断言会把设计系统的类名与结构变成事实上的契约，让每次视觉调整都变成验收返工。

代价就是这份清单：**API 里根本不存在的东西，只能靠人看。** 它如实记账，不假装没有。

**不给退出码。** 自动验收（`run-m2-acceptance.sh`）的 PASS/FAIL 与本清单互不替代：
前者判「规格错没错」，后者判「渲染错没错」。

## 触发条件（两条，固定）

1. **M2 验收时必跑一次。**
2. **任何改动 `docs/design-system/README.md` 或 `docs/design-system/tokens.css` 的变更，合并前必跑一次。**

跑完**贴实际观察，不是贴「通过」**——照 ADR-0014 §8 第 3 条的先例。

## 走查项

制造情形的手段与自动验收同源（ADR-0028 §3.1 的 A1–A14），走查时可直接复用那套编排造出各态。

**一条全局读法（2026-08-21，[ADR-0043](../../../adr/0043-p2-job-center.md)）**：下表凡写「**运行详情**」的，
一律读作「**运行详情抽屉**」——运行历史独立屏取消之后，那些形态整体搬进了作业中心行内点开的右侧抽屉，
形状语义一个没变，只是从哪儿打开变了。凡写「**运行历史列表**」的，见 §V9 / §V14 的改判。

### 一、三条形状轴不得互换（ADR-0025 §3）

| # | 打开哪一屏 · 制造什么情形 | 该看见什么 |
|---|---|---|
| V1 | 运行详情 · 进行中 | 阶段串是**三个圆点** `PREPARING ─▶ STREAMING ─▶ COMMITTING` + 尾部文字「→ 终态待定」。**不是五个**；终态不得画成圆点 |
| V2 | 运行详情 · 成功 | 终态是**块**、`SWAPPED` **实心** |
| V3 | 运行详情 · 校验失败 | 终态是**块**、`DISCARDED` **描边** |
| V4 | 运行详情 · 映射预检失败 | 错误码是**标签**；4xx **虚边**、5xx **实边**；码名右边永远跟中文人话结论 |
| V5 | 灰度打印 / 关掉颜色 | V2–V4 的信息**一条不丢**——形状与边型独立于色彩承载信息。**须贴实测数字**（2026-08-16，[#89](https://github.com/liumingjian/db-qbs/issues/89)）：整页 `filter: grayscale(1)` 截图后取 `SWAPPED` 与 `DISCARDED` 块内中位亮度，**差 ≥ 25/255（≈10%）**；「目视可分」不算通过——那句话在 3.1% 时也为真，两轮走查就是这么放过去的。设计系统 §8 |

### 二、轴二只在墓碑真存在时出现（#58 定形 1，设计系统 §3）

| # | 屏 · 情形 | 该看见什么 |
|---|---|---|
| V6 | ~~运行详情 · 形状预检失败~~ | **N/A（判据已随 ADR-0036 §5 退役）**。形状预检整段取消，这一屏造不出来。编号保留，见 ADR-0040 增补（2026-08-19） |
| V7 | 运行详情 · 映射预检失败 | **不出终态块**。「未创建暂存表」在人话结论条里，不是块（原文写「同上」，指的是已退役的 V6，此处补全） |
| V8 | 运行详情 · 进程消失 / 服务重启 | 「结局不明」族**不出终态块、也不出错误码标签** |
| V9 | ~~运行历史列表~~ | **N/A（判据已随 ADR-0043 §4 退役，且方向反转）**。原判「『结局』列里块与中性灰字并存、视觉不齐，齐了就是假的」——运行历史屏整屏取消，作业中心的「运行状态」列是**一维索引**（成功 / 失败 / 进行中 / 结局不明 / 尚未运行），五个词都是同一种实心方角标签，**齐是对的**。轴二「不齐」这件事整体移交详情抽屉，由 V2 / V3 / V7 / V8 承担。编号保留，见 ADR-0043 增补（2026-08-21） |

### 三、两段预检分开是结构不是文案（#58 第 2 问）

| # | 屏 · 情形 | 该看见什么 |
|---|---|---|
| V10 | ~~运行详情 · 形状预检失败~~ | **N/A（判据已随 ADR-0036 §5 退役）**。只剩一段，`.is-skipped` 占位卡已由 #132 撤掉 |
| V11 | 运行详情 · 映射预检失败 | **前半 N/A**（形状预检那张卡随 ADR-0036 §5 退役）；**后半照跑**：映射预检那张卡按 `列 \| 源端 \| 目标端 \| 规则 \| 建议` 逐列摆 + 末尾总计，「一次报全部」要看得见 |
| V12 | ~~运行详情 · 形状预检失败~~ | **N/A（判据已随 ADR-0036 §5 退役）**。错误码闭集不增不减这件事仍成立，只是不再由这一条守 |

### 四、业务值的呈现（ADR-0025，定价归 ADR-0024）

| # | 屏 · 情形 | 该看见什么 |
|---|---|---|
| V13 | 运行详情 · 哨兵逃逸 | `column` / `value` 在**独立告警框**里、**默认打码**，需要显式动作才展开 |

### 五、两个 id 与并发提示（#58 第 4/5 问）

| # | 屏 · 情形 | 该看见什么 |
|---|---|---|
| V14 | ~~运行历史列表~~ | **N/A（判据已随 ADR-0043 §2 退役）**。原判「`run_record_id` 是第一列、主列、可点；`run_id` 紧随其后」——该列表没了。**两个 id 谁也不替代谁**这件事没退役，改由 V15 在抽屉里守：`run_record_id` 在抽屉标题旁，`run_id` 在「运行参数与标识」里等宽 `--dim` 灰一号。编号保留，见 ADR-0043 增补（2026-08-21） |
| V15 | 运行详情 · **任一未向 sink 发出请求的失败**（形状预检那一态已退役，改用源端连接失败造） | `run_id` 栏位（抽屉「运行参数与标识」里的「**目标端运行号**」）写「**未发起，目标端不知道这次运行**」，**不是空白、不是横杠**；两个栏位谁也不替代谁。**2026-08-21 起本条兼守原 V14 的「两个 id 并存」** |
| V16 | 发起对话框 · 有进行中 run | info 人话条给 `run_record_id` 与已跑时长，并明说提示允许滞后、**只提示不拦你**；**发起按钮不禁用** |
| V17 | 运行详情 · 进行中 | **取消按钮常亮**；点下去当场如实拒绝（禁用状态本身会说谎） |

### 六、构建器与建表 SQL（ADR-0023 / ADR-0027）

| # | 屏 · 情形 | 该看见什么 |
|---|---|---|
| V18 | ~~建任务 · 手改 SQL~~ | **N/A（判据已随 ADR-0036 §2 退役）**。源端 SQL 改由结构化规格现算、只读，手改入口与确认模态一并没了 |
| V19 | ~~建任务 · `target_table` 未填~~ | **N/A（判据已随 [ADR-0043](../../../adr/0043-p2-job-center.md) 退役）**。原判「表名处是 SQL 占位符 `<目标表名>`、SQL 照给不拦着人先看」——构建器的**建表 SQL 区块整段已不存在**（`47a2fed` 摘掉，所有者 2026-08-21 裁定按有意的收窄对待）。第一版不提供建表 SQL，目标表仍由用户自己建。`.ddl-placeholder` 这个组件在设计系统 README §7 里**保留**（生成的源端 SQL 那一处仍可能用到），只是这一条没有对象。编号保留 |
| V20 | ~~建任务 · 取列成功~~ | **N/A（判据已随 [ADR-0043](../../../adr/0043-p2-job-center.md) 退役）**。同 V19：取列卡与它下面那两句说明（「刷新即丢」「`ERROR 1118` 是静态提示不是判定」）随整段一起没了对象。**目标列参考表没退役**——列名 / 类型 / 长度 / 可空 / 约束照给，由 X5 守。编号保留 |
| V21 | ~~建任务 · 目标端两字段~~ | **N/A（判据已被 ADR-0038 §3 / ADR-0039 §5 推翻，方向反了）**。目标表下拉与目标列参考表已明文开出并落地；它们的形态判据归 `v1-visual-walkthrough.md` 的 X5 / X7 |
| V22 | 运行详情 · 映射预检失败 | 只放「回到取列拿建表 SQL」入口，**不在此重给建表 SQL**（双出口会误导人撞 `ERROR 1050`） |
| V23 | 全局 | 零处「重试」运行的措辞，一律「**重新发起**」 |

### 七、导航与排版

| # | 屏 · 情形 | 该看见什么 |
|---|---|---|
| V24 | 全局导航 | **前半照跑**：**构建器不是独立导航项**（运行详情也不是——它是行内点开的抽屉）。**后半 N/A**：「调度只是占位置灰标 `M3+`」的对象在 P0 已被撤掉（非 v1 导航占位整批删除，见 ADR-0042 §背景），此处补记这次静默退役。**新增判据（ADR-0043 §8）**：导航就三项（作业中心 · 数据源 · 系统设置）；侧栏是**深色** `--sider-bg`、宽 256px；顶栏最左的折叠触发器点下去侧栏收到 48px，折叠态**只剩图标且图标居中**（不是被切掉一半的选中块），语义靠 `title` |
| V25 | 任意屏 | 中文走系统字体栈（栈尾补了 CJK 回退，见 tokens.css §排版），数字走独立等宽栈 + `tabular-nums`；强调字重 **500**（2026-08-21 改判：原判「600 不是 700」，现按 x2doris 表头 / 标题块的实测值取 500，ADR-0043 §3）；**没有暗色主题**（ADR-0025 正面否掉）——**深色侧栏不是暗色主题**：内容区、卡片、表格仍是浅色，也不提供主题切换（ADR-0043 §8） |

## 关于 N/A 那几条（2026-08-19 增补，[#133](https://github.com/liumingjian/db-qbs/issues/133)）

**V6 / V10 / V11 前半 / V12 / V18 / V21 六条的对象已被第一版的三条 ADR 拿掉或反向**
（ADR-0036 §5 取消形状预检、ADR-0036 §2 把源端 SQL 改成由规格现算且只读、
ADR-0038 §3 开出目标端元数据面）。裁定与理由落 **ADR-0040 增补（2026-08-19）**，要点三条：

1. **编号一个都不重编、不删行**——同 M2 的 A3/A6 先例，旧走查记录要一直对得上。
2. **走查记录里照实写 `N/A（判据已随 ADR-XXXX 退役）`，不许写「通过」，也不许静悄悄跳过。**
3. **整份 V1–V25 仍是一个整体门禁**：N/A 是判据的状态，不是一次跑的豁免。

## 关于 P2 那次改判（2026-08-21 增补，[ADR-0043](../../../adr/0043-p2-job-center.md)）

作业中心（P2）把运行历史独立屏并进任务列表、并把 `tokens.css` 的取值来源换成对 x2doris 的实测。
**触发条件第 2 条因此命中，整份 V1–V25 必跑。** 落到判据上是六条，处置逐条写在上表里：

| # | 处置 | 为什么 |
|---|---|---|
| V9 | 退役（方向反转） | 列表的运行状态收敛成一维索引，「不齐」不再是它该有的样子 |
| V14 | 退役，判据并入 V15 | 运行历史列表没了，但「两个 id 谁也不替代谁」还在，只是换了地方守 |
| V24 | 后半退役 + 新增外壳判据 | `M3+` 占位项 P0 已删（补记）；侧栏深色 / 折叠是新形态 |
| V25 | 改判 `--weight-em` 600 → 500 | 取值来源从自拟改成实测 |
| V19 / V20 | 退役 | 构建器的建表 SQL / 取列卡整段已不存在（`47a2fed` 摘掉），所有者 2026-08-21 裁定按有意的收窄对待，判据随之判废 |

**先说清楚一件事，免得下一个人白找**：本清单**没有一条判 44px 行高、13.5px 单元格、圆角标签或 `#2C8AF0`**
这类具体数值——它判的自始至终是**形状与语义**。所以 P2 那次令牌重取值（行高 44→41、控件 30→32、
表格字号 13.5→14、标签圆角 3→0、主色 `#2C8AF0`→`#3C7EFF`、卡片去边框、语义四色换 AntD 4 那套）
**本身不会让任何一条挂**。唯一被数值带动的是 V25 的字重，因为那一条**确实写了数字**。

三条规矩照 2026-08-19 那次原样有效：**不重编号、不删行**；记录里照实写 `N/A（判据已随 ADR-XXXX 退役）`，
不许写「通过」也不许静悄悄跳过；**整份 V1–V25 仍是一个整体门禁**。

## 记录格式

每次走查在本仓库开一份 `m2-visual-walkthrough-<UTC>.md`（与 `m2-acceptance-<UTC>.md` 并列），
逐条写实际观察。**「V7 通过」不算记录，「V7：映射预检失败屏无终态块，结论条写『目标表未被触碰』」才算。**
